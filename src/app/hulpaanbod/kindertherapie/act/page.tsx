import { ContentRoutePage } from "@/components/pages/ContentRoutePage";
import { pageMetadata } from "@/content/seo";

export const metadata = pageMetadata.watIsAct;

export default function WatIsActPage() {
  return <ContentRoutePage routeKey="watIsAct" />;
}
